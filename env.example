# Render.com Environment Variables
# Bu dosyayı .env olarak kaydedin ve Render.com'da Environment Variables olarak ekleyin

# Database
DATABASE_URL=postgresql://username:password@host:port/database

# JWT Secret
JWT_SECRET=imalattakip-secret-key-2024-render

# Node Environment
NODE_ENV=production

# Port (Render.com otomatik ayarlar)
PORT=3001

# CORS Origins (Render.com URL'inizi ekleyin)
CORS_ORIGINS=https://your-app-name.onrender.com,http://localhost:3000
